<!-- newsfeed.hbs -->
{{#each news}}
<section class="news-item">
    <a href="#"><img src="{{this.image}}" alt="News Image"></a>
    <div class="news-content">
        <h3><a href="{{this.link}}">{{this.title}}</a></h3>
        <p>{{this.description}}</p>
    </div>
</section>
{{/each}}
